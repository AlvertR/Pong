namespace Pong
{
    public enum GameStatus
    {
        Start,
        Playing,
        Paused,
        PointEnd,
        End
    }

    public enum Player
    {
        Unknown,
        One,
        Two
    }
}
